/**
 * Limites do Tom do escritório (amostras → resumo de estilo).
 * Alinhado a MAX_MODELOS_TOM (1 modelo ativo por espécie, até 12).
 */

export const MAX_AMOSTRAS_ESTILO = 12;
export const MIN_AMOSTRAS_ESTILO = 1;
